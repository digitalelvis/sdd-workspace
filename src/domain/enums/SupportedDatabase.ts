export enum SupportedDatabase {
  POSTGRES = "postgres",
  MONGODB = "mongodb",
  MYSQL = "mysql",
  SQLITE = "sqlite",
  REDIS = "redis",
  SUPABASE = "supabase",
  FIREBASE = "firebase",
  NONE = "none"
}
